import SwiftUI

struct GraphView: View {
    var body: some View {
        Text("Graph")
    }
}

struct GraphView_Previews: PreviewProvider {
    static var previews: some View {
        GraphView()
    }
}
