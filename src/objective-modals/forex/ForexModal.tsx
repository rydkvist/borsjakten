import React from 'react';
import { View, Text, Modal } from 'react-native';
import { ObjectiveModal } from '../../common';

const ForexModal = () => {
  return (
    <View>
      <Text>Forex Modal</Text>
      <ObjectiveModal />
    </View>
  );
};

export default ForexModal;
