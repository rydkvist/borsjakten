import React from 'react';
import { View, Text, Modal } from 'react-native';
import { ObjectiveModal } from '../../common';

const CryptoModal = () => {
  return (
    <View>
      <Text>Crypto Modal</Text>
      <ObjectiveModal />
    </View>
  );
};

export default CryptoModal;
