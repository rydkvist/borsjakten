import React from 'react';
import { View, Text, Modal } from 'react-native';
import { ObjectiveModal } from '../../common';

const SummerJobsModal = () => {
  return (
    <View>
      <Text>Summer Jobs Modal</Text>
      <ObjectiveModal />
    </View>
  );
};

export default SummerJobsModal;
