import React from 'react';
import { View, Text, Modal } from 'react-native';
import { ObjectiveModal } from '../../common';

const RealEstateModal = () => {
  return (
    <View>
      <Text>Real Estate Modal</Text>
      <ObjectiveModal />
    </View>
  );
};

export default RealEstateModal;
