import React from 'react';
import { View, Text, Modal } from 'react-native';
import { ObjectiveModal } from '../../common';

const CSNModal = () => {
  return (
    <View>
      <Text>CSN Modal</Text>
      <ObjectiveModal />
    </View>
  );
};

export default CSNModal;
